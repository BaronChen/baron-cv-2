// import { actionTypes } from './my-navbar.actions';

export const initialNavbarState  = {
}

export const myNavbar = (state  = initialNavbarState, action)  => {
    switch (action.type) {
        default:
            return state;
    }
}